jenkins:
  remotingSecurity:
    enabled: true
  systemMessage: ${aws.toolkit}
support:
  automatedBundleConfiguration:
    period: 24
unclassified:
  datadogGlobalConfiguration:
    ciInstanceName: ${HOSTNAME}
