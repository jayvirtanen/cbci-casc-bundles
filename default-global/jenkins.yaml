jenkins:
  remotingSecurity:
    enabled: true
  systemMessage: ${MASTER_ENDPOINT}
support:
  automatedBundleConfiguration:
    period: 24
unclassified:
  datadogGlobalConfiguration:
    ciInstanceName: ${HOSTNAME}
