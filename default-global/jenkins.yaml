jenkins:
  remotingSecurity:
    enabled: true
  systemMessage: ${MASTER_DOMAIN}
support:
  automatedBundleConfiguration:
    period: 24
unclassified:
  datadogGlobalConfiguration:
    ciInstanceName: ${HOSTNAME}
