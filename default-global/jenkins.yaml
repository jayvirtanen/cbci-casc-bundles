jenkins:
  remotingSecurity:
    enabled: true
  systemMessage: ${LOCATION}
support:
  automatedBundleConfiguration:
    period: 24
unclassified:
  datadogGlobalConfiguration:
    ciInstanceName: ${HOSTNAME}
